<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rafflefoods">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RaffleFoods">
        <activity android:name=".LotteryActivity"></activity>
        <activity android:name=".RestaurantActivity" />
        <activity
            android:name=".BeitouActivity"
            android:label="@string/beitou_district" />
        <activity
            android:name=".WuguActivity"
            android:label="@string/wugu_district" />
        <activity
            android:name=".LinkouActivity"
            android:label="@string/linkou_district" />
        <activity
            android:name=".XinzhuangActivity"
            android:label="@string/xinzhuang_district" />
        <activity
            android:name=".SanchongActivity"
            android:label="@string/sanchong_district" />
        <activity
            android:name=".LuzhouActivity"
            android:label="@string/luzhou_district" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>